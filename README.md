# CLIP Text Vectorizer

Convert JSON data to CLIP text embeddings. Processes JSON objects/arrays, extracts text fields, and generates normalized embeddings using OpenAI's CLIP model.

## Features
- Batch processing for efficiency
- Multiple output formats (NPZ, JSON)
- Configurable model and batch size
- Automatic GPU detection
